# proxy boilerplate for yamma-server backend

## How to set it up

git clone https://github.com/MettyS/yamma-server-proxy.git

npm install

npm run dev - npm start is not currently configured with nodemon to ignore output file changes